<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>inventory</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    </head>
    <body>
        <div class="jumbotron text-center">
            <h1>Hello! <%= name %></h1>

        </div>
        <div class="container-fluid">
            <div class="row ml-md-auto">
                <a href="/logout" class="btn btn-primary pull-right" role="button">Log out</a>
                <a href="/create" class="btn btn-primary" role="button">Create new inventory</a>
            </div>
            <div class="row">
                <p>Search inventory name:
                <label for="bar"><span class="glyphicon glyphicon-search"></span></label><input style="width:100%" type="text" id="myInput" onkeyup="myFunction()" placeholder="Search name:" name="bar"><br></p>
                        <script> var inventory = <%- JSON.stringify(inventory)%>; 
						 for (var i=0;i<<%=ninventory%>;i++){ 
							document.write(
							"inv_id : " + inventory[i]["inv_id"]+"<br>"+
							"name : " + inventory[i]["name"]+"<br>"+
							"inv_type : " + inventory[i]["inv_type"]+"<br>"+
							"quantity : " + inventory[i]["quantity"]+"<br>"+
							"description : " + inventory[i]["description"]+"<br>"+
							"owner : " + inventory[i]["owner"]+"<br>");
						}
						</script>
            </div>
        </div>
        <script>
        $(document).ready(function(){
            $("#myInput").on("keyup", function() {
                var value = $(this).val().toLowerCase();
                $("#myTable tr").filter(function() {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
                });
            });
        });

        </script>
    </body>
</html>
