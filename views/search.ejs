<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>inventory</title>
        
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
        <script>
        function goBack() { window.history.back();}
        </script>
        <style>
            img {
              max-width: 100%;
              height: auto;
            }
        </style>
    </head>
    <body>
        <div class="jumbotron text-center">
            <h1>Details of <%= name %></h1>
        </div>
		<script> var inventory = <%- JSON.stringify(inventory)%>; 
		 for (var i=0;i<<%=ninventory%>;i++){ 
			document.write(
			'<div class="container-fluid"><div class="well"><div class="row"><img src="data:'+inventory[i]["phototype"]+';base64,'+inventory[i]["photo"] +
			'"><br></div><div class="row">'+
			"<p>inv_id : " + inventory[i]["inv_id"]+"</p><br>"+
			"<p>name : " + inventory[i]["name"]+"</p><br>"+
			"<p>inv_type : " + inventory[i]["inv_type"]+"</p><br>"+
			"<p>quantity : " + inventory[i]["quantity"]+"</p><br>"+
			"<p>description : " + inventory[i]["description"]+"</p><br>"+
			"<p>owner : " + inventory[i]["owner"]+"</p><br></div></div>");
		}
		</script>
		<div class="row ml-md-auto">
			<button class="btn btn-primary" onclick="goBack()">Back</button>
		</div>
		<br>
    </body>
</html>
